#include "life.h"

int HEIGHT = SIZE;
int WIDTH = SIZE;
int FIELD_SIZE = SIZE * SIZE / 4 + 2; // +2 пустые поля
void next_gen(int field[]) {
    for (int i = 1; i < ; i++) {
        int c = field[i];
        int W = field[i - 1];
        int E = field[i + 1];
        int N = field[]
    }
}   